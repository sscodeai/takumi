{
  "name": "takumi",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source extensible Agentic Software Engineering Platform with first-class support for Japanese enterprise software development.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck"
  },
  "bin": {
    "takumi": "apps/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}