{
  "name": "takumi",
  "version": "0.1.0",
  "private": true,
  "description": "Model-agnostic platform for verifiable agentic software delivery.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck"
  },
  "bin": {
    "takumi": "apps/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
