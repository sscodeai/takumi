// Service layer — INCOMPLETE (to be implemented by agent).
import { items, nextItemId } from './data.js';

export class InventoryService {
  // TODO: implement register/reserve/restock/list
  register({ name, price }) {
    // stub
  }
  reserve(id, quantity) {
    // stub
  }
  restock(id, quantity) {
    // stub
  }
  list() {
    // stub
  }
}
